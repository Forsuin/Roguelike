#pragma once

#include "color.h"

namespace yarl {
	namespace colors {

		const color_t WHITE(255);
		const color_t BLACK(0);
		const color_t RED(255, 0, 0);
		const color_t GREEN(0, 255, 0);
		const color_t BLUE(0, 0, 255);
	}
}